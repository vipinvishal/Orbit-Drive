# Server-only — read by next.config.ts's rewrite to proxy /api/* through
# to the backend. Never exposed to the browser (no NEXT_PUBLIC_ prefix),
# and the frontend code never needs to know the backend's real URL.
BACKEND_ORIGIN=http://localhost:8000
